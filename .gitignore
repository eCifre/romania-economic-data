# Kaggle credentials — never commit these.
kaggle.json
.kaggle/
KAGGLE_USERNAME
KAGGLE_KEY

# Hugging Face credentials — never commit these.
HF_TOKEN
.huggingface/

# Zenodo credentials — never commit these.
ZENODO_ACCESS_TOKEN

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS noise
.DS_Store

# Publish staging (assembled and removed by the publish scripts each run —
# never manually maintained, generated fresh from build/ every time).
kaggle/.publish_stage/
huggingface/build/
zenodo/build/
